import React from 'react'

export const substrateInitialValues = {

}

export default function Substrate({values, handleChange}) {
    return (
        <div>
            {/* Material will be written later*/}
        </div>
    )
}
